---
description: File the current answer or analysis into the vault as a durable page.
argument-hint: "[title]"
---

# Save to the wiki

File the durable result of our current conversation into the wiki so it does not die in chat history, per the Query operation's "file durable answers back" rule in AGENTS.md.

Title: $ARGUMENTS

Follow the wiki-query skill's filing steps — a new page under `comparisons/` (or a better-fitting category), then the index, log, and pulse updates. If no title is given, propose one from the content and confirm it with me.
