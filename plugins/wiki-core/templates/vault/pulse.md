# Pulse

Recent-context cache (~500 words). The agent rewrites this in prose each session, ingest, and durable query — it is not appended to. Read this first to resume without a recap; it complements the `index.md` catalog and the `log.md` timeline, it does not replace them.

## Working context

_(empty)_

## Recently updated

_(empty)_
