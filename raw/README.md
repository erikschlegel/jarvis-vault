# Raw sources

Immutable source documents. The agent reads from here but does not edit these files.

- Drop articles, papers, transcripts, and clipped notes in `sources/`.
- Store downloaded images and attachments in `assets/`.

Use the Obsidian Web Clipper or paste markdown files directly into `sources/`.